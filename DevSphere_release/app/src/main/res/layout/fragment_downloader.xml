<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/ds_bg_dark" android:padding="16dp">

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="📥 HLS / Video Downloader"
        android:textColor="@color/ds_text_primary" android:textSize="20sp"
        android:textStyle="bold" android:layout_marginBottom="20dp" />

    <EditText android:id="@+id/etStreamUrl"
        android:layout_width="match_parent" android:layout_height="56dp"
        android:hint="Paste HLS or video URL…"
        android:background="@drawable/input_bg" android:padding="14dp"
        android:textColor="@color/ds_text_primary"
        android:textColorHint="@color/ds_text_muted"
        android:inputType="textUri" android:layout_marginBottom="12dp" />

    <Button android:id="@+id/btnStartDownload"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:text="Start Download"
        android:backgroundTint="@color/ds_accent_blue"
        android:textColor="@color/ds_text_primary"
        android:textStyle="bold" android:layout_marginBottom="16dp" />

    <ProgressBar android:id="@+id/downloadProgress"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent" android:layout_height="8dp"
        android:max="100" android:progressTint="@color/ds_accent_blue"
        android:visibility="gone" android:layout_marginBottom="12dp" />

    <TextView android:id="@+id/tvDownloadStatus"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:textColor="@color/ds_text_secondary" android:textSize="13sp" />

    <FrameLayout android:id="@+id/upgradeOverlay"
        android:layout_width="match_parent" android:layout_height="match_parent"
        android:background="#CC0D0D0F" android:visibility="gone">
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="🔒 Developer plan required\nUpgrade to use Downloader"
            android:textColor="@color/ds_text_primary" android:gravity="center"
            android:textSize="16sp" />
    </FrameLayout>
</LinearLayout>
