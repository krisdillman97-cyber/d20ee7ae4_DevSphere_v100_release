<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardBackgroundColor="@color/ds_bg_card" app:cardCornerRadius="10dp">
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:padding="12dp" android:gravity="center_vertical">
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical">
            <TextView android:id="@+id/tvProvider"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="Provider" android:textColor="@color/ds_accent_gold"
                android:textSize="13sp" android:textStyle="bold" />
            <TextView android:id="@+id/tvMaskedKey"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="sk-****" android:textColor="@color/ds_text_secondary"
                android:textSize="12sp" android:fontFamily="monospace" />
        </LinearLayout>
        <ImageButton android:id="@+id/btnDeleteKey"
            android:layout_width="36dp" android:layout_height="36dp"
            android:src="@android:drawable/ic_menu_delete"
            android:backgroundTint="@android:color/transparent"
            android:tint="@color/ds_accent_red" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
